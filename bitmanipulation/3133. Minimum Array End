class Solution {
public:
    long long minEnd(int n, int x) {
        long long ans = x;
        //have t0o set lsb to 1\
        do or operation with x to get answer
        for (int i = 1; i < n; i++) {
            ans = (ans + 1) | x;
        }
        return ans;
    }
};
